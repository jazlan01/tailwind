{
  "name": "tailwind-demo",
  "version": "1.0.0",
  "description": "A repository to practice and reference tailwind designs",
  "main": "index.js",
  "scripts": {
    "dev": "npx live-server",
    "watch": "npx tailwindcss -i ./styles.css -o ./output.css --watch",
    "build": "npx tailwindcss -o output.css --minify",
    "format": "prettier --write . && prettier --write ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss-cli": "^10.1.0",
    "prettier": "^3.0.0",
    "prettier-plugin-tailwindcss": "^0.4.1",
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
